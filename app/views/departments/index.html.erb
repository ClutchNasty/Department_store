<h1><h1>Departments</h1>
<%= link_to 'Create a new Department', new_department_path %>
<%= link_to 'Edit Department', edit_department_path(@departments) %>
<br />

<ul>
  <% @departments.each do |department| %>
<%= link_to department.name, department_items_path(department) %>

  
  <% end %>
</ul></h1>
